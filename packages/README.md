# Packages

Shared packages for `nextop-apps`.

Create a package here when code is reused by more than one app or needs an
app-independent contract.

Recommended package examples:

- `@nextop-apps/config`: shared TypeScript, Tailwind, or lint config.
- `@nextop-apps/github`: GitHub REST client and API types.
- `@nextop-apps/trend-classification`: reusable taxonomy and scoring logic.
- `@nextop-apps/ui`: shared UI primitives only if multiple apps need the same
  components.

Read `AGENTS.md` before editing or adding packages.
