# Getting Started CLI

The app exposes the `onboarding` CLI scope for read-only status checks.

## Commands

```bash
tutti --json onboarding status
```

All commands return JSON in the Tutti `CliCommandOutput` envelope.
