# Tutti Onboarding CLI

The app exposes a read-only `onboarding` CLI scope.

## Commands

```bash
tutti --json onboarding status
```

The command returns a JSON envelope with package readiness and the main guide
sections included in the onboarding app.
