{
  "name": "@tutti-apps/tutti-onboarding",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev --host 127.0.0.1 --port 3003",
    "test": "node scripts/check-assets.mjs",
    "typecheck": "node scripts/check-assets.mjs"
  },
  "dependencies": {
    "vite": "^8.0.16"
  }
}
